# AGENTS.md

## 项目目标
这是一个手机优先的逐日训练器，用于导入 12 周训练计划，并按日执行训练、计时、记录重量、保存备份。

## 核心原则
1. 手机端优先，iPhone Safari / Edge 必须可用。
2. 不要破坏已有 localStorage 数据结构；如需改结构，必须写迁移函数。
3. 不要因为训练内容里出现“休息：90秒”就把训练日分类为“休息/恢复”。
4. 导入 Excel 时优先读取“手机查看版 / 一日一格 / 逐日执行”工作表。
5. 必须跳过：导航、使用说明、周总览、替代规则、先看这里。
6. 每次修改后不要删功能：今日训练、训练队列、计时器、动作库、热身库、RM换算、备份导入导出都要保留。
7. 日期逻辑以实际训练发生日期为准：`actualDate > scheduledDate > plannedDate`。训练日志和总存档必须优先使用 `actualDate`，计划原始日期只能作为参考。

## 文件职责
- index.html：页面结构入口
- style.css：样式与手机适配
- app.js：主交互、页面切换、训练执行
- importer.js：Excel 文件导入
- parser.js：训练内容解析
- storage.js：localStorage、备份导入导出、数据迁移
- templates.js：训练动作库与热身动作库

## 导入兼容目标
必须兼容：
1. 12周逐日执行训练计划_徐晖版_手机清晰版
2. 12周逐日执行训练计划_肖悦 2

## 测试重点
每次修改后至少检查：
1. 手机导航可以切换：今日训练 / 日历队列 / 训练记录 / 参数设置 / RM换算 / 导入适配
2. 导入徐晖版不会全部识别成休息/恢复
3. 导入肖悦版可以生成逐日训练
4. 训练队列能进入指定训练
5. 完成训练后可以导出总存档
6. 倒计时结束提示正常
7. 同名动作能显示上次重量参考
