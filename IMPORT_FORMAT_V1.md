# Training Tracker Structured Import v1

本文档是外部计划生成工具与 training-tracker 之间的唯一数据接口规范。它只定义 Excel 数据结构、校验和导入行为，不包含训练内容设计规则。

## 1. 格式识别与分流

固定主工作表名：

```text
训练器数据_v1
```

- 工作簿存在 `训练器数据_v1`：必须进入 Structured Import v1，按本文档严格校验。
- 工作簿不存在 `训练器数据_v1`：才允许进入 Legacy Import。
- Structured Import 校验失败：立即终止，不得回退 Legacy Import、近似解析或内置计划。
- 读取和预览不会修改当前 Program 或 localStorage。

Legacy Import 继续兼容徐晖旧版、肖悦旧版、`手机查看版_一日一格` 和旧逐日执行 Excel。

## 2. 主表：训练器数据_v1

第一行列名必须逐字一致。每一行表示一个 Exercise。

| 列名 | 类型 | 必填 | 规则 |
|---|---|---:|---|
| `schemaVersion` | 文本/整数 | 是 | 固定为 `1` |
| `programName` | 文本 | 是 | 同一工作簿所有行必须一致 |
| `workoutId` | 文本 ID | 是 | 稳定且合法；同一 Workout 的动作共用同一 ID |
| `顺序` | 正整数 | 是 | Workout 显示顺序；不同 workoutId 不得重复 |
| `plannedDate` | 日期文本 | 否 | 留空或 `YYYY-MM-DD`；仅作计划参考 |
| `训练主题` | 文本 | 是 | 同一 workoutId 必须一致 |
| `section` | 枚举 | 是 | 见“section 枚举” |
| `exerciseId` | 文本 ID | 是 | 在整个工作簿中唯一且稳定 |
| `动作顺序` | 正整数 | 是 | 同一 workoutId 内不得重复 |
| `动作名称` | 文本 | 是 | 真实动作名称；不能是 section 标题或标记 |
| `组数` | 正整数 | 是 | 至少为 `1` |
| `次数下限` | 非负数字 | 否 | 与次数上限同时填写或同时留空 |
| `次数上限` | 非负数字 | 否 | 不得小于次数下限 |
| `RIR下限` | 0-10 数字 | 否 | 与 RIR 上限同时填写或同时留空 |
| `RIR上限` | 0-10 数字 | 否 | 不得小于 RIR 下限 |
| `建议重量` | 非负数字 | 否 | 只写数值，不包含 `kg` 或 `lb` |
| `单位` | 枚举 | 条件必填 | 只允许 `kg`、`lb` 或留空；填写建议重量时必填 |
| `休息下限秒` | 非负数字 | 否 | 与休息上限秒同时填写或同时留空 |
| `休息上限秒` | 非负数字 | 否 | 不得小于休息下限秒 |
| `动作秒数` | 正数 | 否 | 时间型动作使用，单位固定为秒 |
| `动作备注` | 文本 | 否 | 原样保存，不做自然语言推断 |
| `超级组ID` | 文本 | 否 | 同一 Workout 内同组动作填写相同值 |
| `是否热身` | 布尔值 | 是 | 只允许 `是/否`、`true/false`、`1/0` |

Structured Import 直接读取以上字段，不调用 `parser.js` 的动作名称、次数、RIR、重量或时间正则。

## 3. section 枚举

只允许以下值：

```text
功能模块
主项
主辅助
辅助
核心
康复/辅助
有氧
恢复
休息
```

以下值是 section 标题，不是 Exercise：

```text
【功能模块】
【主项】
【主辅助】
【辅助】
【核心】
```

它们出现在 `动作名称` 时属于严重错误，不能生成动作卡。

## 4. 可选表：组计划_v1

`组计划_v1` 只描述与主表默认处方不同的 Set。不存在该工作表时，所有 Set 都生成 `working`。

| 列名 | 类型 | 必填 | 规则 |
|---|---|---:|---|
| `workoutId` | 文本 ID | 是 | 必须对应主表中的 workoutId |
| `exerciseId` | 文本 ID | 是 | 必须与 workoutId 一起精确对应主表动作 |
| `组号` | 正整数 | 是 | 从 `1` 开始，不能超过主表组数，同一动作内不得重复 |
| `setType` | 枚举 | 是 | 见“setType 枚举” |
| `次数下限` | 非负数字 | 否 | 成对填写；覆盖该组默认值 |
| `次数上限` | 非负数字 | 否 | 成对填写；不得小于下限 |
| `RIR下限` | 0-10 数字 | 否 | 成对填写；覆盖该组默认值 |
| `RIR上限` | 0-10 数字 | 否 | 成对填写；不得小于下限 |
| `休息下限秒` | 非负数字 | 否 | 成对填写；覆盖该组默认值 |
| `休息上限秒` | 非负数字 | 否 | 成对填写；不得小于下限 |
| `重量调整类型` | 枚举 | 否 | `percent`、`absolute` 或留空 |
| `重量调整值` | 数字 | 条件必填 | 与重量调整类型同时填写或同时留空 |
| `技术提示` | 文本 | 否 | 该组专属提示，原样保存 |

## 5. setType 枚举

只允许以下小写英文值：

```text
working
technique
warmup
top
backoff
dropset
```

训练器不会根据动作名称、section 或备注猜测 setType。

## 6. 稳定 ID 规则

`workoutId` 和 `exerciseId`：

- 必须以英文字母开头。
- 后续只允许英文字母、数字、下划线 `_` 和连字符 `-`。
- 最长 64 个字符。
- 移动训练顺序后 workoutId 不变。
- 移动动作顺序后 exerciseId 不变。
- exerciseId 在整个工作簿中唯一，推荐包含 workoutId。
- 数组 index 不是身份。

合法示意：

```text
W001
W001-E01
```

非法示意：

```text
训练 1
001-E01
W001/E01
```

## 7. RIR、重量与时间

### RIR

范围必须拆成两个数字列。例如 RIR 2-3：

```text
RIR下限 = 2
RIR上限 = 3
```

不得填写 `RIR2-3`、`余力2-3` 等自然语言。

### 重量

建议重量和单位必须分列：

```text
建议重量 = 62.5
单位 = kg
```

`62.5kg` 属于非法建议重量。建议重量是计划字段，不是用户实际训练重量；实际重量继续写入训练草稿和 workoutLogs。

### 时间

所有时间字段均使用秒数：

```text
休息下限秒 = 90
休息上限秒 = 120
动作秒数 = 20
```

不得填写 `1分30秒`、`2分钟`、`20s`。

## 8. 特殊组覆盖规则

1. 先根据主表的组数、次数、RIR 和休息生成全部 `working` Set。
2. 以 `workoutId + exerciseId` 找到该动作的组计划。
3. 再按 `组号` 精确覆盖对应 Set。
4. 组计划留空字段继续沿用主表默认值。
5. 未被组计划提及的 Set 保持 `working`。
6. 组计划不得新增超过主表组数的 Set。

协议示意：主表某动作有 4 组；组计划将第 1 组标记为 `technique`，第 4 组标记为 `backoff` 且 `percent = -15`。生成结果依次为：

```text
technique
working
working
backoff -15%
```

## 9. 时间型动作规则

时间型动作在主表填写：

```text
组数 = 2
次数下限 = 留空
次数上限 = 留空
动作秒数 = 20
```

导入后动作及其每个 Set 的 `duration` 都是 `20`。训练器不会从动作名称或备注推断时长。

## 10. 超级组规则

- 同一 Workout 中属于同一超级组的动作填写相同 `超级组ID`。
- 一个非空超级组ID在同一 Workout 中至少关联两个 Exercise。
- 超级组ID不跨 Workout 建立关联。
- 超级组只表达动作关联，不改变 exerciseId、动作顺序、组数或 setType。
- 训练器不会从动作名中的 `+`、`连做` 或 `超级组`文字进行猜测。

## 11. 严格校验清单

正式生成预览前必须检查：

1. `训练器数据_v1` 存在且表头完整。
2. schemaVersion 为 `1`。
3. programName 一致。
4. workoutId、exerciseId 存在且格式合法。
5. exerciseId 全工作簿唯一。
6. 同一 workoutId 的顺序、plannedDate、训练主题一致。
7. Workout 顺序和同日动作顺序不重复。
8. section、是否热身属于固定枚举。
9. 动作名称不是 section 标题或标记。
10. 组数、次数、RIR、休息、动作秒数和建议重量合法。
11. 上下限字段成对填写且上限不小于下限。
12. setType 和重量调整类型属于固定枚举。
13. 组计划引用存在的 workoutId + exerciseId。
14. 组号不超过动作组数且不重复。
15. 超级组ID在同日至少关联两个动作。

任一严重错误都会抛出 `ImportError`，提示“无法识别该训练计划，没有修改当前训练计划”。当前 Program、其他 Program 和 localStorage 保持原样。

## 12. 合法与非法行示意

合法主表行只展示协议结构，不代表训练建议：

```text
1 | 外部计划 | W001 | 1 | 2026-09-01 | 训练日A | 主项 | W001-E01 | 1 | 动作A | 3 | 8 | 10 | 2 | 3 | 20 | kg | 90 | 120 |  |  |  | 否
```

合法特殊组行：

```text
W001 | W001-E01 | 3 | backoff | 10 | 12 | 1 | 2 | 60 | 90 | percent | -15 | 技术提示文本
```

非法示意：

```text
section = 肩部动作                 // 非固定枚举
动作名称 = 【主项】                // section 标记，不是动作
建议重量 = 62.5kg                  // 数值和单位未分列
RIR下限 = RIR2-3                   // 自然语言，不是数字
休息下限秒 = 1分30秒               // 不是秒数
setType = heavy                    // 非固定枚举
组号 = 5，但主表组数 = 4           // 超出范围
```

## 13. Program 写入行为

- 校验成功只生成标准预览对象，不自动写入。
- 用户明确点击“导入为新的训练计划”并确认后，创建新的 Program。
- 新 Program 使用主表 programName，保留稳定 workoutId、exerciseId 和 setId。
- 其他 Program 的 currentWorkout、actualDates、completed、currentWorkoutDrafts、workoutLogs 和 exerciseHistory 不变。
- “替换当前计划”是独立操作，必须再次确认。
