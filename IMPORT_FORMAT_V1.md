# Training Tracker Structured Import v1

本文档是 `训练器标准训练计划_v1.xlsx` 的唯一格式规范。未来生成、校验和导入结构化训练计划时，以本文档为准。

## 识别规则

工作簿中只要存在名为 `训练器数据_v1` 的工作表，训练器就进入 Structured Import v1 模式。

- 结构化模式优先于所有旧格式。
- 结构化校验失败时终止导入，不回退到旧解析器。
- 失败不会创建、替换或修改任何 Program。
- 没有 `训练器数据_v1` 时，才尝试徐晖版、肖悦版、手机查看版、一日一格和逐日执行旧格式。

## 训练器数据_v1

必需工作表。第一行必须完整保留以下固定列名，每一行代表一个动作。

| 列名 | 类型 | 规则 |
|---|---|---|
| schemaVersion | 文本 | 固定为 `1` |
| programName | 文本 | 计划名称，同一文件通常一致 |
| workoutId | 文本 | 训练日稳定 ID，同一训练日的动作相同 |
| 顺序 | 正整数 | 训练日在 Program 中的顺序 |
| plannedDate | 日期/文本 | 原始计划日期，仅作参考 |
| 训练主题 | 文本 | 训练日标题 |
| section | 枚举 | 见下方固定 section |
| exerciseId | 文本 | 动作稳定 ID，在同一 workoutId 内唯一 |
| 动作顺序 | 正整数 | 动作在训练日中的顺序 |
| 动作名称 | 文本 | 真实动作名，不能填写 `【主项】` 等分区标记 |
| 组数 | 正整数 | 要生成的组数 |
| 次数下限 | 数字/空 | 默认目标次数下限 |
| 次数上限 | 数字/空 | 默认目标次数上限，不能小于下限 |
| RIR下限 | 0-10/空 | 默认 RIR 下限 |
| RIR上限 | 0-10/空 | 默认 RIR 上限，不能小于下限 |
| 建议重量 | 文本/数字/空 | 计划参考重量，不是实际训练重量 |
| 单位 | 文本 | 通常为 `kg` 或 `lb` |
| 休息下限秒 | 非负数字/空 | 默认休息区间下限 |
| 休息上限秒 | 非负数字/空 | 默认休息区间上限 |
| 动作秒数 | 非负数字/空 | 按时间执行动作时使用 |
| 动作备注 | 文本/空 | 技术要求或执行备注 |
| 超级组ID | 文本/空 | 同一超级组填写相同 ID |
| 是否热身 | 布尔文本 | `是/否`、`true/false` 或 `1/0` |

固定 section 仅允许：

```text
功能模块
主项
主辅助
辅助
核心
康复/辅助
有氧
恢复
休息
```

`【主项】`、`【主辅助】` 等标题只能表达分区，不能作为动作名称，也不会生成动作卡。

## 组计划_v1

可选工作表。只填写与动作默认处方不同的特殊组。未填写的组自动生成 `working` 工作组。

| 列名 | 类型 | 规则 |
|---|---|---|
| workoutId | 文本 | 必须对应 `训练器数据_v1` |
| exerciseId | 文本 | 必须对应同一 workoutId 下的动作 |
| 组号 | 正整数 | 不得超过动作的组数 |
| setType | 枚举 | 见下方六种固定类型 |
| 次数下限 | 数字/空 | 覆盖该组默认值 |
| 次数上限 | 数字/空 | 覆盖该组默认值 |
| RIR下限 | 0-10/空 | 覆盖该组默认值 |
| RIR上限 | 0-10/空 | 覆盖该组默认值 |
| 休息下限秒 | 非负数字/空 | 该组目标休息下限 |
| 休息上限秒 | 非负数字/空 | 该组目标休息上限 |
| 重量调整类型 | 枚举/空 | `percent` 或 `absolute` |
| 重量调整值 | 数字/空 | 例如 `-15` 配合 percent 表示下降 15% |
| 技术提示 | 文本/空 | 该组专属提示 |

setType 仅允许：

```text
working   工作组
technique 技术组
warmup    热身组
top       顶组
backoff   回退组
dropset   递减组
```

示例：第三组是回退组并降重 15%。

```text
w001 | ex001 | 3 | backoff | 5 | 5 | 2 | 3 | 150 | 180 | percent | -15 | 动作质量优先
```

训练器显示为：`回退组｜5次｜RIR 2-3｜休 150-180秒｜↓15%`。

## 数据边界

- 计划处方保存在 Workout/Exercise/Set 中。
- 用户实际输入的重量、次数、RIR 保存在当前草稿，完成训练后追加到 `workoutLogs`。
- 计划建议重量不等于实际训练重量。
- `exerciseHistory` 从 `workoutLogs` 重建，导入计划不会覆盖历史日志。
- 实际日期优先级仍为 `actualDate > scheduledDate > plannedDate`。

## 下载模板

在训练器的“导入适配”页面点击“下载标准计划模板”，会生成：

```text
训练器标准训练计划_v1.xlsx
```

其中包含 `填写说明`、`训练器数据_v1` 和 `组计划_v1` 三个工作表及可直接修改的示例。
